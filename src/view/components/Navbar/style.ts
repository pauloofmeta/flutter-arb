import styled from 'styled-components';

export const Nav = styled.nav`
    width: 100%;
`;

export const Title = styled.div`
    font-weight: bold;
    padding: 10px 0px;
    font-size: 23px;
`;

export const NavActions = styled.div`
    display: flex;
    flex-direction: row;
    justify-content: space-between;
`;

export const NavActionsRight = styled.div`
    display: flex;
    flex-direction: row;
    gap: 5px;
`;